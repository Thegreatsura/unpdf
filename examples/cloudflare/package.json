{
  "name": "@unpdf/example-cloudflare",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "esbuild --bundle --platform=neutral --outfile=build/index.js index.ts && wrangler dev build/index.js"
  },
  "devDependencies": {
    "esbuild": "^0.25.12",
    "wrangler": "^4.51.0"
  }
}
